sudo: required
services:
  - docker


before_install:
  - docker build -t vpryshchepa/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true vpryshchepa/docker-react npm run test -- --coverage
